services:
  - type: web
    name: retail-intelligence-api
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn retail_api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
